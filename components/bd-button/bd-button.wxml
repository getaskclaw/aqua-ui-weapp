<view
  class="bd-btn bd-btn-{{variant}} bd-btn-{{size}} {{disabled || loading ? 'bd-btn-off' : ''}}"
  hover-class="{{disabled || loading ? 'none' : 'bd-btn-hover-' + variant}}"
  bindtap="onTap"
>
  <view wx:if="{{loading}}" class="bd-btn-spinner"></view>
  <slot />
</view>
