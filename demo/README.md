# Demo moved to the repository root

The former placeholder has been replaced by a complete six-page MiniProgram showroom for all fifty-five native components. Import the repository root into WeChat DevTools; the startup page is `pages/home/home`.
